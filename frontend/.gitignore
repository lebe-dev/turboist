node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Backup written by `just cap-sync-versioned` while the version stamp is applied;
# only present if a build was interrupted mid-flight.
package.json.stamp-backup
