services:
  turboist:
    image: tinyops/turboist:0.4.1
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./data:/app/data
    restart: unless-stopped
