# The database tests need a real SQLite engine, not the toolchain's newest API
# level. Pinning the level keeps them running on whatever platform images the
# test runtime happens to have, and the replica uses no API newer than this.
sdk=34
